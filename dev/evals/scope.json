{
  "skill": "scope",
  "mode": "comprehension",
  "evals": [
    {
      "id": "spec-structure",
      "prompt": "We want to add rate limiting to our public API to stop abuse. Spec this change.",
      "fixture": "a repo with src/api/server.js with no rate limiting",
      "assertions": [
        "The interview climbs above the stated solution: it asks what was observed and pressure-tests the premise before speccing rate limiting",
        "The stated solution becomes one alternative among others in the research section, as the first decision",
        "The change is sized small or full, the choice is told to the user with a reason, and the user can override",
        "The spec is written to .dev/{plan-name}/spec.md with a research section of D- slugged decisions using the marks (chosen, rejected, open, accepted downside, not doing)",
        "The scope section ends with a Validation block listing the repo's real commands, discovered rather than guessed",
        "The spec does not implement code; no source files are modified",
        "The run ends by recommending build; it is never invoked directly",
        "Every change set ends with one tests: line whose scenarios carry [unit], [integration], or [e2e] tags, or tests: none with a reason",
        "scripts/lint-spec.py is run against the written spec and looped on until it exits clean"
      ]
    },
    {
      "id": "review-and-ledger",
      "prompt": "We want to add rate limiting to our public API to stop abuse. Spec this change.",
      "fixture": "the same repo, with docs/decisions.md containing a not-doing entry for rate limiting whose reopen condition mentions abuse volume",
      "assertions": [
        "A subagent reviewer receives the spec file only, not the conversation, with a hunting job rather than a checklist",
        "The drafted decisions are reconciled against docs/decisions.md only in phase 5, after they were argued fresh",
        "The collision with the recorded not-doing entry is classified (still-holds, diverged, or reopened) and raised rather than silently overridden",
        "The retrospective reports the three typed lines: contamination, sizing, catalog gaps"
      ]
    },
    {
      "id": "jira-disabled",
      "prompt": "We want to add rate limiting to our public API to stop abuse. Spec this change.",
      "fixture": "a repo with src/api/server.js and no .dev/config.json, with no acli executable on PATH",
      "assertions": [
        "The transcript contains no Jira, acli, Initiative, Epic, or issue-tracker mention",
        "No acli invocation occurs and the spec is written using the normal local workflow"
      ]
    },
    {
      "id": "remediation-from-review",
      "prompt": "Take the accepted findings from the last review and turn them into new change sets.",
      "fixture": "a repo with .dev/rate-limit/spec.md holding change sets 1-3, plus review_1.md and review_2.md with Blockers, Concerns, deferred findings, and a Decision reconciliation section",
      "assertions": [
        "The plan directory is located by the convention in references/plan-layout.md rather than guessed",
        "review_2.md is used, not review_1.md, because it is the highest-numbered review",
        "The accepted Blockers and Concerns open the interview as decisions rather than being implemented directly",
        "Rejected and deferred findings are written as not-doing lines with reopen conditions, not dropped",
        "New change sets are appended to the existing spec.md starting at 4; the existing change sets are never renumbered",
        "A defect change set carries the review's triggering scenario as its reproduction",
        "The review's Decision reconciliation section is applied to docs/decisions.md"
      ]
    },
    {
      "id": "reverse-mode-audit",
      "prompt": "Audit the decisions embedded in the sync layer.",
      "fixture": "a repo with src/sync/ containing hardcoded timeouts, a retry count, and a page size, and a docs/decisions.md with one recorded entry about sync retries",
      "assertions": [
        "No spec.md is written; reverse mode produces ledger entries, not a spec",
        "The catalog pass inventories embedded decisions before judging any of them, including values that look fine",
        "Each entry gets the problem it was solving, or is recorded as an unexamined default with no known problem",
        "The recorded retry entry is verified against the current code and a mismatch is raised as diverged rather than silently overwritten",
        "A decision worth changing is recommended as a normal scope run and never launched directly"
      ]
    },
    {
      "id": "scope-confidence-threshold",
      "prompt": "Answer briefly, from the skill text only. During the interview you recommend storing the dedup keys in a database table over Redis at Confidence: 82%, and separately recommend a 30-day retention window at Confidence: 55%. Later a finding suggests the real problem is not duplicate deliveries but a missing retry, at Confidence: 90%. (0) Do you ask about the store, and how is that decision recorded? (1) Do you ask about the retention window? (2) Do you ask about the retry finding, and why? (3) When does the user see the recommendations you applied without asking?",
      "assertions": [
        "Answer 0: no question; the table is applied and its chosen line carries `auto-applied at Confidence: 82%` in its because clause",
        "Answer 1: yes, 55% is below the 75% threshold, so the user is asked",
        "Answer 2: yes, whatever the score, because it changes what was asked for (a different problem than the request names)",
        "Answer 3: once, in one block at the end of the interview, so a single reply can overturn any of them before the spec is written"
      ]
    }
  ]
}
