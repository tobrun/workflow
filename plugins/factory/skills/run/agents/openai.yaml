interface:
  display_name: "Run"
  short_description: "Take a request from scope to a shipped change unattended"
  default_prompt: "Use $factory:run to take a request through scope, then run scope-review, build, and ship unattended."
policy:
  allow_implicit_invocation: false
